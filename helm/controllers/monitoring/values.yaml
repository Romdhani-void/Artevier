grafana:
  enabled: true
  adminPassword: "changeme123"
  service:
    type: ClusterIP

prometheus:
  enabled: true
  prometheusSpec:
    retention: 1d
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1Gi

alertmanager:
  enabled: false