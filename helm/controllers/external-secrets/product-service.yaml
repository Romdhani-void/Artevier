apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: product-service-secret
  namespace: artevier
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: product-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: artevier/dev/product-service