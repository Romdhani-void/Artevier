apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: notification-service-secret
  namespace: artevier
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: notification-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: artevier/dev/notification-service