apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: frontend-secret
  namespace: artevier
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: frontend-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: artevier/dev/frontend