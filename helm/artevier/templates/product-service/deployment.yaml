apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: artevier
spec:
  replicas: {{ (index .Values.services "product-service").replicas }}
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: "{{ (index .Values.services "product-service").image }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ (index .Values.services "product-service").targetPort }}
          envFrom:
            - secretRef:
                name: product-service-secret