apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: artevier
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - port: {{ (index .Values.services "api-gateway").port }}
      targetPort: {{ (index .Values.services "api-gateway").targetPort }}
      protocol: TCP