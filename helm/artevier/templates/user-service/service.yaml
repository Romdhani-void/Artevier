apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: artevier
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: {{ (index .Values.services "user-service").port }}
      targetPort: {{ (index .Values.services "user-service").targetPort }}
      protocol: TCP